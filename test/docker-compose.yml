version: "2.1"

services:
  web:
    image: cheppers/phpenv:7.4
    ports:
      - "8080:80"
    volumes:
      - ".:/project/"
    environment:
      APACHE_RELATIVE_DOCROOT: public
      XDEBUG_MODE: "develop,coverage,debug"
